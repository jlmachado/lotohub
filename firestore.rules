rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita total durante a fase de migração e seeding
    // Isso garante que o LocalStorage seja espelhado no banco sem erros de permissão
    match /{document=**} {
      allow read, write: if true;
    }
  }
}