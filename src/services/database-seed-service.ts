'use client';

/**
 * @fileOverview Dummy service para evitar quebra de imports legados.
 */

export class DatabaseSeedService {
  static async ensureInitialData() {
    // Reversão concluída: Não utiliza mais banco remoto.
    return Promise.resolve();
  }
}
