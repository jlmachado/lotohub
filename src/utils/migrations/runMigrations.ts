'use client';

/**
 * @fileOverview Dummy migration runner para evitar quebra de imports legados.
 */

export async function runMigrations() {
  // Reversão concluída: Migrações para Firebase desativadas.
  return Promise.resolve();
}
